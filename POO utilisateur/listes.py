#!/usr/bin/python3.4

def p(*args):
    print(*args)


liste = list() #On déclare la liste
liste = [] #On crée une liste vide
liste = [2 , 2.5, 'hello', []] #On peut aussi lui attribuer directement des valeurs tel qu'un chiffre, un flottant, un string, ou même une autre liste
